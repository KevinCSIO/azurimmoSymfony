{% extends 'base.html.twig' %}

{% block title %}Contrat{% endblock %}

{% block body %}
    <div class="card-detail">
        <h1>Contrat</h1>

        <div class="info">
            <p><strong>Date d'Entrée :</strong> {{ contrat.dateEntree|date('d/m/Y') }}</p>
            <p><strong>Date de Sortie :</strong> {{ contrat.dateSortie|date('d/m/Y') }}</p>
            <p><strong>Montant du Loyer :</strong> {{ contrat.montantLoyer }}</p>
            <p><strong>Montant des Charges :</strong> {{ contrat.montantCharges }}</p>
            <p><strong>Statut :</strong> {{ contrat.statut }}</p>

        </div>

        <div class="actions">
            <a href="{{ path('app_contrat_index') }}" class="btn btn-primary">Retour</a>
            <a href="{{ path('app_contrat_edit', {'id': contrat.id}) }}" class="btn btn-primary">Modifier</a>
            {{ include('contrat/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}




