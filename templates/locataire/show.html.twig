{% extends 'base.html.twig' %}

{% block title %}Locataire{% endblock %}

{% block body %}
    <div class="card-detail">
        <h1>Locataire</h1>

        <div class="info">
            <p><strong>Nom :</strong> {{ locataire.nom }}</p>
            <p><strong>Prénom :</strong> {{ locataire.prenom }}</p>
            <p><strong>Date de Naissance :</strong> {{ locataire.dateN|date('d/m/Y') }}</p>
            <p><strong>Lieu de Naissance :</strong> {{ locataire.lieuN }}</p>
        </div>

        <div class="actions">
            <a href="{{ path('app_locataire_index') }}" class="btn btn-primary">Retour</a>
            <a href="{{ path('app_locataire_edit', {'id': locataire.id}) }}" class="btn btn-primary">Modifier</a>
            {{ include('locataire/_delete_form.html.twig') }}
        </div>
    </div>
 {% endblock %}



